import styled from 'styled-components';

const StyledContainer = styled.button`
  position: fixed;
  width: 100px;
  height: 30px;
  border-radius: 50%;
`;

const ShowCommentListButton = () => {
  return <StyledContainer />;
};

export default ShowCommentListButton;
